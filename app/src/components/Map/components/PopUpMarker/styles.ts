import styled from 'styled-components'

const PopupItem = styled.div`
  display: flex;
  gap: 10px;
  font-size: 16px;
`

const PopupLabel = styled.div`
  font-weight: bold;
`

const PopupLabelValue = styled.div``

export const styles = {
  PopupItem,
  PopupLabel,
  PopupLabelValue
}
